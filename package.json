{
  "name": "terraform-erosion-sandbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A From-Dust-style god game: sculpt 3D terrain and watch realistic water flow, erosion, and lava cooling into rock.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.3.4",
    "wrangler": "^3.0.0"
  }
}
